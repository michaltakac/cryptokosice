{
  "name": "cryptokosice-web",
  "version": "2.0.0",
  "private": true,
  "homepage": "./",
  "scripts": {
    "start": "hugo serve",
    "pin": "node ./scripts/pin.js",
    "build": "hugo -d build/ --gc --minify",
    "deploy": "npm run build && npx ipfs-deploy build/ -p pinata -d cloudflare -O"
  },
  "devDependencies": {
    "@pinata/sdk": "^1.1.6"
  }
}
